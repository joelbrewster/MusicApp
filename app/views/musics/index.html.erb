<!-- <p id="notice"><%= notice %></p>

<h1>Listing Musics</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Document</th>
      <th>Audio</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->

  <!-- <tbody>
    <% @musics.each do |music| %>
      <tr>
        <td><%= music.image %></td>
        <td><%= music.document %></td>
        <td><audio id = "audio" autoplay>
              <%= music.audio %>
            </audio>
        </td>
        <p class="<%=music.id%>"><%= link_to image_tag(music.image.url(:thumb)), music %> </p>

        <td><%= link_to 'Show', music %></td>
        <td><%= link_to 'Edit', edit_music_path(music) %></td>
        <td><%= link_to 'Destroy', music, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Music', new_music_path %>


<ul class="collection">
  <li class="collection-item avatar">

    <% @musics.each do |music| %>
      <span class="title"><%=music.audio_file_name%></span>
      <p>Description <br>
         This is how I feel
      </p>
      <a href="#!" class="secondary-content"><i class="material-icons">grade</i></a>
    <%end%>
  </li>
</ul> -->



<ul class="collection with-header ul-music-list">
  <% @musics.each do |music| %>
    <li class="collection-item li-music"><div><%=music.audio_file_name%>
      <%= link_to music, class: 'secondary-content music-link' do %>
        <i class="material-icons">play_arrow</i>
      <%end%>
    </li>
  <% end %>
</ul>
